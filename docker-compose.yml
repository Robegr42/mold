services:
  agent:
    build: .
    image: gensie-mold-team
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PARTICIPANT_PATH=${PARTICIPANT_PATH:-gensie.baseline.OfficialParticipant}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FASTEMBED_CACHE_PATH=/app/models
